# ============================================================
# refinex-user 服务配置
# 发布到 Nacos: Group=REFINEX_SERVICE
# ============================================================

server:
  # User 服务端口
  port: 8082

spring:
  servlet:
    multipart:
      # 头像上传单文件大小限制
      max-file-size: 5MB
      # 请求总体大小限制
      max-request-size: 10MB
