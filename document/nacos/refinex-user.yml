# ============================================================
# refinex-user 服务配置
# 发布到 Nacos: Group=REFINEX_SERVICE
# ============================================================

server:
  # User 服务端口
  port: 8082

# 当前 refinex-user 暂无额外专属业务参数。
# 若后续新增用户服务参数（如注册策略/缓存键前缀等），请继续维护本文件。
